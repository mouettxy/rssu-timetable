{
    "scripts": {
        "dev:server": "cd server && npm run dev",
        "dev:client": "cd client && npm run dev",
        "dev:db": "docker-compose up -d",
        "dev": "npm-run-all dev:db --parallel dev:server dev:client"
    },
    "dependencies": {
        "npm-run-all": "^4.1.5"
    }
}
